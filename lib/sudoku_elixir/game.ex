defmodule SudokuElixir.Game do
  def solve(board) do
    # if Board.valid?(board) do
    #   for move <- Enum.to_list(1..board.board_size) do
    #     board
    #     |> move(move, cell)
    #     |> solve
    #   end
    # end
  end

  defp move(board, move, cell) do

  end
end
